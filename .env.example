DATABASE_URL="postgresql://user:password@host:5432/db?sslmode=require"

# Auth.js – obligatoire en production
AUTH_SECRET="générer avec: openssl rand -base64 32"

# En prod (Vercel) : définir l’URL du site pour les callbacks OAuth
# AUTH_URL="https://ton-app.vercel.app"

# OAuth – optionnel (Google)
AUTH_GOOGLE_ID=""
AUTH_GOOGLE_SECRET=""

# OAuth – optionnel (GitHub)
AUTH_GITHUB_ID=""
AUTH_GITHUB_SECRET=""